import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.csrushrush.mathrush3d',
  appName: 'Math Rush 3D',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
